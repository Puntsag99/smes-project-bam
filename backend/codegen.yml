overwrite: true
schema: "./schema"
generates:
  ./types/generated.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      makeResolverTypeCallable: true
      maybeValue: T | null | undefined
